# 真实论文首版交付基准

**结论：6 篇真实论文，0 篇可以直接交付。**

数据来自 `benchmarks/results/first-delivery.json`，由
`benchmarks/run_first_delivery_benchmark.py` 跑出，可重跑：

```
python3 benchmarks/run_first_delivery_benchmark.py \
    --work-dir <临时目录> --real-translation real-translation
```

论文与作业目录受版权保护，不进仓库；结果文件里只有哈希、页数和派生结论。

## 语料的诚实边界

| 译文来源 | 篇数 | 说明 |
| --- | --- | --- |
| 真实模型译文 | 1 | `real-translation` |
| 确定性合成译文 | 5 | 仓库自带语料，只为触发代码路径，**不代表译文质量** |

原文全部是真实的开放获取论文。两类混在一张表里报，数字就没意义了，
所以脚本按来源分开统计。

## 逐篇结果

| 作业 | 页数 | 结论 | 重建 | 问题 | 待人工 | 卡在哪一步 |
| --- | ---: | --- | ---: | ---: | ---: | --- |
| real-image-heavy | 12 | blocked | 0 | 1 | 0 | 输入就绪：字体覆盖缺口 |
| real-reference-heavy | 75 | blocked | 0 | 1 | 0 | 输入就绪：字体覆盖缺口 |
| real-single-column-body | 12 | handover | 1 | 14 | 8 | 走完全链，返修部分生效 |
| real-table-and-model | 15 | blocked | 0 | 1 | 0 | 输入就绪：字体覆盖缺口 |
| real-translation | 8 | handover | 1 | 26 | 16 | 走完全链，返修部分生效 |
| real-two-column-body | 12 | blocked | 0 | 1 | 0 | 输入就绪：字体覆盖缺口 |

### 逐阶段对比

| | blocked | handover | delivered | real-translation 剩余问题 |
| --- | ---: | ---: | ---: | ---: |
| 阶段 15（管线未接） | 6 | 0 | 0 | 32 |
| 阶段 15b（接元素管线） | 4 | 2 | 0 | 29 |
| 阶段 15c（接图题绑定） | 4 | **2** | 0 | **26** |

两篇能走完整条链的论文，从"返修完全无效"变成"返修部分生效"。
仍然 0 篇可交付。

## 两类失败

### 一、字体覆盖缺口（4 篇）

`FONT_CHARACTER_COVERAGE_GAP`。合成译文里带着从原文抄来的数学符号
（`∈` U+2208，以及 U+F8EE 这类私用区字符），冻结字体画不出来，
候选里会退化成空字符，所以生成前就被拦下。

这是预渲染检查按设计工作。它拦的是合成译文的取材方式，不是排版缺陷。

### 二、返修部分生效，剩下的交给人（2 篇）

以 `real-translation` 为例，返修修好了两条、没修好四条、零回退：

```
修好: p0003-figure-001（整张丢失的矢量图）、p0004-formula-002（丢失的公式）
仍缺: 四条正文单元，它们的可用文字只剩单个数学字体残渣字符，判不了
弄坏: 无
```

阶段 15c 之后，这一篇的**结构对账只剩一条**：那四条判不了的正文单元。
图题与图分页（3 处）已经全部消失，阅读顺序逆序对也回到门槛内。

## 接管线做了什么（阶段 15b）

上一轮查出：`build_candidate.py` 全文 6405 行里 `render_plan` 出现 0 次，
整条元素管线没有接进生产生成器，返修算出的降级生成器根本看不见。

这一轮补上缺的三段，一段一段加，旧路径一行没删：

1. **翻译层** `academic_pdf_translation/render/plan_bridge.py`：读渲染计划，
   把定到保留级的元素翻成生成器已经认识的复杂内容条目。只翻保留这两级，
   其余策略仍走原路。缺坐标就如实跳过，不猜一个框。
2. **渲染层** `build_candidate.py` 新增保留级的分发与栅格化：不低于 300 DPI，
   且**不放大**——原区域在版面上占多少点就画多少点，放不下才等比缩小。
3. **核查层** `candidate_mapping` 新增像素指纹定位：保留下来的区域在候选里
   是一张图片，没有文字层也没有绘图对象，文字锚点和几何数量都看不见它。
   把两边都降采样成 16×16 灰度比一比就知道是不是同一块。
   真实样本上保留区域与原区域差 1.3–3.6，不相干的区域差 20 以上，
   门槛定在 8.0。几乎全白的区域不比对——两块空白永远长得一样。

这一段的关键在于第 3 点不是"相信生成器说它保留了"，而是自己去看候选页面上
画的是什么。

## 图题跟着图走（阶段 15c）

阶段 15b 之后仍有 3 处图题与图分在不同页，对应人工复审 R-006。
两类情况分别处理：

- **保留区域的图题**：翻译层从元素关系里取到图题译文挂在保留条目上，
  渲染时和图锁成一块。
- **四联子图的图级图题**：每格自己的 (a)(b)(c)(d) 说明本来就内嵌在条目里，
  但整张图的图题（"图 3. ……"）是一条独立的译文单元，排在正文流里，
  随时会被分到上一页或下一页。现在按图像 xref 把它挂到对应的复杂条目上，
  同样锁成一块，正文里重复的那一份自动抑制。

矢量图没有 xref，这条路认不出来——如实跳过，不按页码猜，猜错会把图题
挂到别的图上。

### 顺带修好一个判据缺陷

阅读顺序逆序对原来把"量不到纵坐标"当成 0，等于宣称"它在这一页最上面"。
同一页里两个元素只要有一个量不到坐标，这一对就被编出了一个顺序。

改成：同页且任一方量不到坐标时跳过，既不算逆序也不进分母；页码不同的一对
按页码比就够了。真实样本上可比对数只掉 4%（4350 → 4163），逆序对却从
265 降到 170——**判据变准了，不是变松了**。

## 未验证的部分

- **翻译性能未验证。** 本轮没有调用真实模型翻译，耗时与 Token 一概没有测量，
  也不做任何估计。表中的秒数只是核查与生成的耗时，不含翻译。
- **10 篇的目标未达到。** 仓库里有 8 篇真实论文、6 个可跑作业，
  其余论文没有配套作业；补齐需要真实模型翻译，本轮无法进行。
- **保留区域目前是栅格，不是矢量。** 分辨率不低于 300 DPI，但放大到很大
  仍会看出像素。矢量保留需要在输出后用 PyMuPDF 二次贴图，留待后续。
- **矢量图的图题绑定未覆盖。** 按图像 xref 对应，矢量图没有 xref。
  样本论文里矢量图走的是保留路径，图题已由另一条路挂上；
  但重建路径下的矢量图图题仍可能与图分页。
