# 变更记录

本文件只写实测过的事实。凡是没有实测支撑的效果数字，一律不写。

## 1.2.0

这一版的主题是：**不再让生成器给自己打分。**

上一版生成器预检通过就返回 `READY_TO_REGISTER`。后来把一份
`READY_TO_REGISTER` 的产物交给独立复审，人看完判了不合格，列出 11 条问题：
整张结构图消失、表格被压成一行流水文字、图题与图分在两页、图内标签被排成
章节标题。这一版围绕那份复审结论重建了核查与交付。

### 新增：核查层（`academic_pdf_translation/verify/`）

只看产出的 PDF，不看计划、不看任何人写进 JSON 的状态。

- **候选元素映射**：每个源元素真的落在候选哪一页。四条判据各有证明力——
  图像字节哈希、像素指纹（16×16 灰度，认得出被保留成图片的区域）、
  文字探针、文字锚点。绘图对象数量**只能证明"不在"**，单独用时置信度只给
  0.30，不假装是定位。
- **结构对账**：逐类型清点、阅读顺序逆序对、图题是否与图同页、页数增长。
- **高风险视觉检查**：把风险信号折成分数排页，只渲染最值得看的几页，
  逐页写清楚要看什么。被预算砍掉的页原样列出——悄悄截断会让报告读起来
  像"全看过了"。
- **一次内部返修**：最多一轮，第二次调用一条都不修。允许的动作只有降级与
  重排；`lower-threshold`、`widen-whitelist`、`skip-check`、`drop-element`、
  `relax-qa`、`mark-complete` 出现即抛错，没有开关可以放行。

### 新增：统一首次交付入口

```bash
python3 scripts/deliver_first_candidate.py <作业目录>
```

只给三种结论，退出码就是结论：`delivered`(0)、`handover`(2)、`blocked`(1)。
证据（两轮的映射、对账、视觉检查计划、返修计划与前后对比、待人细看的页面
图片）全部落盘。**没有证据的结论不算结论。**

返修跑完却一个字没改时，会比对返修前后的内容哈希并报 `blocked`，
写明"降级指令没有落到生成器上"——报成"修了没修好"，读的人会以为已经试过了。

### 新增：11 个渲染器与三级降级链

公式不重新输入；表格六项条件全确定才允许结构化重建，否则保留原表区域加
中文翻译键，**绝不压成段落**；位图不放大；文字角色只来自元素清单，
不看字号也不看行长。重建不可靠时退到"保留原文那一块"，再不行退到
"保留整张原文页"。

### 新增：两个可重跑的基准

- `benchmarks/run_benchmark.py`：性能基准，绑定 `renderer_build_id`；
  报告哈希与源码哈希对不上，`tests/test_benchmark_provenance.py` 就失败。
- `benchmarks/run_first_delivery_benchmark.py`：真实论文首版交付基准，
  按译文来源分开统计，跑不动的记为"未验证"并点名。

### 实测结果

6 篇真实开放获取论文（原文不随仓库分发），其中 1 篇是真实模型译文，
5 篇是确定性合成译文（只触发代码路径，**不代表译文质量**）：

| | delivered | handover | blocked |
| --- | ---: | ---: | ---: |
| 首版交付 | **0** | 2 | 4 |

- 4 篇 blocked 卡在生成前的字体覆盖检查——合成译文里带着从原文抄来的 `∈`
  与私用区字符，冻结字体画不出来。这是检查按设计工作。
- 2 篇走完整条链，返修部分生效后停在 handover。

管线接进生成器的过程分三步，逐步改善：

| | blocked | handover | 单篇剩余问题 |
| --- | ---: | ---: | ---: |
| 管线未接 | 6 | 0 | 32 |
| 接元素管线 | 4 | 2 | 29 |
| 接图题绑定 | 4 | 2 | 26 |

完整数据见
[benchmarks/results/first-delivery.md](benchmarks/results/first-delivery.md)。

### 修复

- **干净安装后按 README 跑一遍必然失败。** `check_bundle` 与 `self_test`
  要审计"交付物里有没有字节码缓存"，可它们自己一导入模块就生成
  `__pycache__`，于是新用户看到的是 `SELF TEST FAIL`。现在两个工具
  关闭字节码写入，不再弄脏它们正在检查的目录。
- **8 个引用包的脚本按文档写法跑不起来。** `python3 scripts/X.py` 时
  `sys.path` 里没有仓库根。这类失败在 pytest 里看不见——conftest 早把路径
  铺好了，所以新增的测试用子进程跑。
- **降级链一直是摆设。** `preserve-element-region` 与
  `preserve-full-source-page` 从来没进过策略到渲染器的映射表，真降下来是
  `KeyError`。补上映射，并加了一条通用不变量测试。
- **阅读顺序判据把"量不到纵坐标"当成 0**，等于宣称"它在这一页最上面"。
  改成同页且任一方无坐标时跳过。真实样本上可比对数只掉 4%，
  逆序对从 265 降到 170——判据变准了，不是变松了。
- **图题与图分页**：保留区域的图题从元素关系取得并与图锁成一块；
  四联子图的图级图题按图像 xref 挂到复杂条目上。真实论文上 3 处全部消失。

### 已知限制

- **首版直接可交付的比例，实测是 0/6。** 这不代表译文没用——停在
  handover 的产物内容是全的。它代表的是：**目前不能无人值守地直接出终稿。**
- **保留区域是栅格，不是矢量。** 不低于 300 DPI 且不放大，但放得很大仍会
  看出像素。矢量保留需要在输出后二次贴图，尚未实现。
- **矢量图的图级图题绑定覆盖不到。** 按图像 xref 对应，矢量图没有 xref。
  不按页码猜——猜错会把图题挂到别的图上，比不挂更糟。
- **翻译性能未验证。** 本套基准不调用真实模型翻译，耗时与 Token 一概
  未测量，也不做估计。
- **只有 1 篇语料是真实模型译文。**

### 文档

新增 [references/element-pipeline.md](references/element-pipeline.md)：
包结构导览、数据流、三条设计原则的代码位置、已知限制。
[references/validation.md](references/validation.md) 开头区分
`READY_TO_REGISTER`（生成器自评）与 `delivered`（核查层结论）。
`tests/test_documentation_claims.py` 盯着文档里的数字不许漂离实测。

## 1.1.0 及更早

见 git 历史。
