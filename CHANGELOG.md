# 变更记录

## 1.2.0

### 新增

- **交付前自动核查**：交付前逐元素核对产出的 PDF——图、表、公式、图题
  是否都在、都在该在的页，核查证据（元素定位、结构对账、待细看页面的
  渲染图）写入作业目录的 `delivery/`，供按档位安排的复审直接使用。
- **统一交付入口**：`python3 scripts/deliver_first_candidate.py <作业目录>`
  一条命令完成核查、一轮自动返修、再核查，退出码即结论
  （0 交付 / 2 转复审 / 1 阻止）。
- **安全保底渲染**：重建不可靠的图、表、公式自动改为原样保留原文区域
  （不低于 300 DPI、不放大），内容不丢失。
- **参考文献整理**：行末断词按文档词表判定合并，被折行切断的 URL
  重新拼接、不插空格。

### 改进

- 图题与它说明的图固定同页，放不下时整块一起换页。
- 表格只在网格与行列关系完全确定时才结构化重建，否则保留原表区域并附
  中文表题与列头对照，不再压成普通段落。
- 文字的标题层级只取自原文结构分析，不再按字号猜测。

### 修复

- 全新安装后按 README 运行自检会误报失败的问题。
- 部分脚本无法按文档写法 `python3 scripts/<名称>.py` 直接运行的问题。
- 版本号在打包配置与插件清单间可能不一致的问题（打包检查现在会校验
  CHANGELOG 版本）。

## 1.1.0 及更早

见 git 历史。
